---
import { baseUrl } from '@/utils/functions';

//@ts-nocheck
const arrayCards = [
  {
    title: 'Plasma',
    repo: 'https://github.com/20essentials/project-1104',
    localImage: '/assets/project-1104.avif',
    id: '001'
  },
  {
    title: 'Lambdas',
    repo: 'https://github.com/20essentials/project-1080',
    localImage: '/assets/project-1080.avif',
    id: '002'
  },
  {
    title: 'Beziers',
    repo: 'https://github.com/20essentials/project-1113',
    localImage: '/assets/project-1113.avif',
    id: '003'
  },
  {
    title: 'Dragabble',
    repo: 'https://github.com/20essentials/project-1115',
    localImage: '/assets/project-1115.avif',
    id: '004'
  },
  {
    title: 'Splitted Text and Menu',
    repo: 'https://github.com/20essentials/project-1088',
    localImage: '/assets/project-1088.avif',
    id: '005'
  },
  {
    title: 'Tip',
    repo: 'https://github.com/20essentials/project-1174',
    localImage: '/assets/project-1174.avif',
    id: '006'
  },
  {
    title: 'Table Rows',
    repo: 'https://github.com/20essentials/project-1175',
    localImage: '/assets/project-1175.avif',
    id: '007'
  },
  {
    title: 'Resinas',
    repo: 'https://github.com/20essentials/project-1075',
    localImage: '/assets/project-1075.avif',
    id: '008'
  },
  {
    title: 'Dynamic Toggle',
    repo: 'https://github.com/20essentials/project-1179',
    localImage: '/assets/project-1179.avif',
    id: '009'
  },
  {
    title: 'Bytes and Gradients',
    repo: 'https://github.com/20essentials/project-1095',
    localImage: '/assets/project-1095.avif',
    id: '010'
  },
  {
    title: 'Split Flap',
    repo: 'https://github.com/20essentials/project-1180',
    localImage: '/assets/project-1180.avif',
    id: '011'
  },
  {
    title: 'Pills',
    repo: 'https://github.com/20essentials/project-1181',
    localImage: '/assets/project-1181.avif',
    id: '012'
  },
  {
    title: 'Random Stroke',
    repo: 'https://github.com/20essentials/project-1085',
    localImage: '/assets/project-1085.avif',
    id: '013'
  },
  {
    title: 'React Led Matrix',
    repo: 'https://github.com/20essentials/project-1182',
    localImage: '/assets/project-1182.avif',
    id: '014'
  },
  {
    title: 'Rotalos',
    repo: 'https://github.com/20essentials/project-1105',
    localImage: '/assets/project-1105.avif',
    id: '015'
  },
  {
    title: 'Input Spark',
    repo: 'https://github.com/20essentials/project-1185',
    localImage: '/assets/project-1185.avif',
    id: '016'
  },
  {
    title: 'Crustales',
    repo: 'https://github.com/20essentials/project-1110',
    localImage: '/assets/project-1110.avif',
    id: '017'
  },
  {
    title: 'MorphSvg Plugin',
    repo: 'https://github.com/20essentials/project-1188',
    localImage: '/assets/project-1188.avif',
    id: '018'
  },
  {
    title: 'Consecutive Scrub',
    repo: 'https://github.com/20essentials/project-1189',
    localImage: '/assets/project-1189.avif',
    id: '019'
  },
  {
    title: 'Waves',
    repo: 'https://github.com/20essentials/project-1072',
    localImage: '/assets/project-1072.avif',
    id: '020'
  }
];
---

<section class='container-cards'>
  <div class='container'>
    {
      arrayCards.map(({ id, localImage, repo, title }) => (
        <article class='card' data-anchor={repo}>
          <div class='glows' />
          <img
            draggable='false'
            class='my-image'
            src={baseUrl(localImage)}
            alt={title}
            title={title}
          />
        </article>
      ))
    }
  </div>
</section>

<script>
  document.addEventListener('click', e => {
    const target = e.target as HTMLElement;
    if (target.matches('.card, .card *')) {
      const card = target.closest('article');
      const link = card?.dataset?.anchor;
      if (link) window.open(link, '_blank');
    }
  });
</script>

<style>
  .my-image {
    position: absolute;
    width: 100%;
    height: 100%;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%) scale(0.8);
    object-fit: cover;
    border-radius: inherit;
  }

  :root {
    --bg: hsl(246 44% 7%);
    --border: hsl(280 10% 50% / 1);
    --card: hsl(237 36% 10%);
    --color: hsl(240 18% 80%);
    --border-width: 0.13vmax;
    --border-radius: 0.78vmax;
    --gradient: conic-gradient(
      from 180deg at 50% 70%,
      hsla(0, 0%, 98%, 1) 0deg,
      #eec32d 72.0000010728836deg,
      #ec4b4b 144.0000021457672deg,
      #709ab9 216.00000858306885deg,
      #4dffbf 288.0000042915344deg,
      hsla(0, 0%, 98%, 1) 1turn
    );
  }

  @property --start {
    syntax: '<number>';
    inherits: true;
    initial-value: 0;
  }

  .container-cards {
    min-height: 100vh;
    color: var(--color);
    background: var(--bg);
    display: grid;
    place-items: center;
    min-height: 100vh;
    font-family: 'Geist Sans', 'SF Pro Text', 'SF Pro Icons', 'AOS Icons',
      'Helvetica Neue', Helvetica, Arial, sans-serif, system-ui;
    font-weight: 70;
    color: var(--color);
    overflow: hidden;
  }

  .container {
    --spread: 60;
    display: flex;
    flex-wrap: wrap;
    flex-direction: var(--direction);
    gap: calc(var(--gap) * 0.07vmax);
    justify-content: center;
    place-items: center;
    position: relative;
    padding: 2vmax;
    touch-action: none;
  }

  .card {
    --active: 0.15;
    --start: 0;
    height: 100%;
    background: var(--card);
    aspect-ratio: 330 / 400;
    border-radius: var(--border-radius);
    padding: 2.08vmax;
    min-width: 18.23vmax;
    max-width: 18.23vmax;
    gap: 0.26vmax;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  article:is(:hover, :focus-visible) {
    z-index: 2;
  }

  .glows {
    pointer-events: none;
    position: absolute;
    inset: 0;
    filter: blur(calc(var(--blur) * 0.07vmax));
  }

  .glows::after,
  .glows::before {
    --alpha: 0;
    content: '';
    background: var(--gradient);
    background-attachment: fixed;
    position: absolute;
    inset: -0.33vmax;
    border: 10px solid transparent;
    border-radius: var(--border-radius);
    mask: linear-gradient(#0000, #0000),
      conic-gradient(
        from calc((var(--start) - (var(--spread) * 0.5)) * 1deg),
        #000 0deg,
        #fff,
        #0000 calc(var(--spread) * 1deg)
      );
    mask-composite: intersect;
    mask-clip: padding-box, border-box;
    opacity: var(--active);
    transition: opacity 1s;
  }

  article::before {
    position: absolute;
    inset: 0;
    border: var(--border-width) solid transparent;
    content: '';
    border-radius: var(--border-radius);
    pointer-events: none;
    background: var(--border);
    background-attachment: fixed;
    border-radius: var(--border-radius);
    mask: linear-gradient(#0000, #0000),
      conic-gradient(
        from
          calc(
            ((var(--start) + (var(--spread) * 0.25)) - (var(--spread) * 1.5)) *
              1deg
          ),
        hsl(0 0% 100% / 0.15) 0deg,
        white,
        hsl(0 0% 100% / 0.15) calc(var(--spread) * 2.5deg)
      );
    mask-clip: padding-box, border-box;
    mask-composite: intersect;
    opacity: var(--active);
    transition: opacity 1s;
  }

  article::after {
    --bg-size: 100%;
    content: '';
    pointer-events: none;
    position: absolute;
    background: var(--gradient);
    background-attachment: fixed;
    border-radius: var(--border-radius);
    opacity: var(--active, 0);
    transition: opacity 1s;
    --alpha: 0;
    inset: 0;
    border: var(--border-width) solid transparent;
    mask: linear-gradient(#0000, #0000),
      conic-gradient(
        from
          calc(
            ((var(--start) + (var(--spread) * 0.25)) - (var(--spread) * 0.5)) *
              1deg
          ),
        #0000 0deg,
        #fff,
        #0000 calc(var(--spread) * 0.5deg)
      );
    filter: brightness(1.5);
    mask-clip: padding-box, border-box;
    mask-composite: intersect;
  }

  .dg.ac {
    z-index: 99999 !important;
  }
</style>

<script>
  //@ts-nocheck
  import { GUI } from 'https://cdn.skypack.dev/dat.gui';

  const CONTAINER = document.querySelector('.container');
  const CARDS = document.querySelectorAll('article');

  const CONFIG = {
    proximity: 130,
    spread: 150,
    blur: 20,
    gap: 32,
    vertical: false,
    opacity: 0
  };

  const PROXIMITY = 10;

  const UPDATE = event => {
    // get the angle based on the center point of the card and pointer position
    for (const CARD of CARDS) {
      // Check the card against the proximity and then start updating
      const CARD_BOUNDS = CARD.getBoundingClientRect();
      // Get distance between pointer and outerbounds of card
      if (
        event?.x > CARD_BOUNDS.left - CONFIG.proximity &&
        event?.x < CARD_BOUNDS.left + CARD_BOUNDS.width + CONFIG.proximity &&
        event?.y > CARD_BOUNDS.top - CONFIG.proximity &&
        event?.y < CARD_BOUNDS.top + CARD_BOUNDS.height + CONFIG.proximity
      ) {
        // If within proximity set the active opacity
        CARD.style.setProperty('--active', 1);
      } else {
        CARD.style.setProperty('--active', CONFIG.opacity);
      }
      const CARD_CENTER = [
        CARD_BOUNDS.left + CARD_BOUNDS.width * 0.5,
        CARD_BOUNDS.top + CARD_BOUNDS.height * 0.5
      ];
      let ANGLE =
        (Math.atan2(event?.y - CARD_CENTER[1], event?.x - CARD_CENTER[0]) * 180) /
        Math.PI;
      ANGLE = ANGLE < 0 ? ANGLE + 360 : ANGLE;
      CARD.style.setProperty('--start', ANGLE + 90);
    }
  };

  document.body.addEventListener('pointermove', UPDATE);

  const RESTYLE = () => {
    CONTAINER.style.setProperty('--gap', CONFIG.gap);
    CONTAINER.style.setProperty('--blur', CONFIG.blur);
    CONTAINER.style.setProperty('--spread', CONFIG.spread);
    CONTAINER.style.setProperty(
      '--direction',
      CONFIG.vertical ? 'column' : 'row'
    );
  };

  const CTRL = new GUI({
    width: 340
  });
  CTRL.add(CONFIG, 'spread', 10, 180, 1).name('Spread (deg)').onChange(RESTYLE);
  CTRL.add(CONFIG, 'proximity', 10, 180, 1)
    .name('Active Proximity (px)')
    .onChange(RESTYLE);
  CTRL.add(CONFIG, 'gap', 10, 100, 1).name('Gap (px)').onChange(RESTYLE);
  CTRL.add(CONFIG, 'blur', 0, 50, 1).name('Blur (px)').onChange(RESTYLE);
  CTRL.add(CONFIG, 'opacity', 0, 1, 0.01)
    .name('Inactive Opacity')
    .onChange(RESTYLE);
  CTRL.add(CONFIG, 'vertical').name('Vertical Layout').onChange(RESTYLE);

  RESTYLE();
  UPDATE();
</script>
